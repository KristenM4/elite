@inherits UmbracoViewPage

@{
    var siteSettings = Model.GetSiteSettings();
    if (siteSettings is null) return;
}
@if (Umbraco.AssignedContentItem.Url() == "/")
{
    @await Html.PartialAsync("metaDataHomePage")
}
else
{
    @await Html.PartialAsync("metaDataContentPage")
}